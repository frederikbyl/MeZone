{
  "rules": {
    "meZoneEvents" : {
				"$event" : {
          ".read": "auth != null && root.child('meZoneEvents/'+$event+'/uid').val() == auth.uid",
        	".write": "auth != null"
        }
  	}
  }
}
